class Solution {
    public int[] runningSum(int[] nums) {
        int add = 0;
        int sum[] = new int [nums.length];
        for (int i = 0; i<nums.length; i++)
        {
            add += nums[i];
            sum[i] = add;
        }
        return sum;
    }
}
